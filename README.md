# trailblazer
A Node.js Server for printing off custom labels

---

1. SmartSuite pings the website with the JSon data
2. The server will check the whitelist for apprpriate network or device
3. The server code will parse and reformat the json data
4. The server will be connected to the printer via USB to print the label
